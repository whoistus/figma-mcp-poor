{
  "name": "figma-mcp-poor",
  "version": "0.4.0",
  "private": false,
  "workspaces": [
    "mcp-server",
    "figma-plugin"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "build:server": "npm run build -w mcp-server",
    "build:plugin": "npm run build -w figma-plugin",
    "dev:server": "npm run dev -w mcp-server",
    "publish:server": "npm publish -w mcp-server"
  }
}